{
  "name": "securecorp-rag-test",
  "version": "1.0.0",
  "description": "RAG Security Test Site with Dynamic Comments",
  "scripts": {
    "dev": "serve src",
    "build": "node build.js",
    "start": "serve public",
    "deploy": "vercel --prod",
    "inject-attacks": "node inject-attacks.js"
  },
  "dependencies": {
    "firebase": "^10.7.1"
  },
  "devDependencies": {
    "serve": "^14.0.0",
    "dotenv": "^16.0.0",
    "fs-extra": "^11.1.0"
  }
}